.DS_Store

# Local dev config — holds real cluster creds + preconfigured connector
# secrets. Never commit it; share the sanitized
# kibana.dev.yml.template.example instead (copy it to this path locally).
kibana.dev.yml.template
